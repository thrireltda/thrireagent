FROM python:3.10-slim

RUN apt-get update && apt-get install -y curl socat && rm -rf /var/lib/apt/lists/*

WORKDIR /core

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["/bin/bash", "./DockerRuntimeFile"]